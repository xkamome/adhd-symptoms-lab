# adhd — ADHD 症狀實驗室

一個**可擴充的合集**：把 ADHD 的內在經驗，各自做成可親身體會的小機制／濾鏡，全部放在這一個資料夾。
首頁列出所有機制，點進去玩；可重用的濾鏡放在 `src/shared/`。

調性：純體驗 / 解謎好玩；主題是優雅外衣，但機制本身要讓人「感覺到」那個症狀。

## 結構
```
src/
├─ App.svelte            # 首頁選單（registry 驅動）+ 返回
├─ mechanics/
│   ├─ registry.ts       # 機制清單（加機制只改這裡 + 新資料夾）
│   ├─ hyperfocus/       # 機制一：超專注隧道（找貓）
│   │   ├─ Hyperfocus.svelte
│   │   └─ paintings.ts
│   ├─ outofsight/       # 機制二：不在眼前就消失（種花）
│   │   └─ OutOfSight.svelte
│   ├─ urgency/          # 機制三：最後一秒引擎
│   │   └─ Urgency.svelte
│   └─ lostkeys/         # 機制四：鑰匙放哪了
│       └─ LostKeys.svelte
├─ shared/
│   └─ FocusTunnel.ts    # ★可重用對焦隧道濾鏡（中心超清晰、周邊模糊、停住縮窄變清、可平移/看答案）
└─ assets/                # 公共版權名畫 (Wikimedia)：Bruegel《Netherlandish Proverbs》/《Children's Games》、Bosch《人間樂園》
```

## 機制一：超專注隧道 (Hyperfocus / 找貓)

**經驗**：超專注時你看得無比仔細，但視野縮成一條隧道，錯過周邊發生的一切。

**玩法**：
- 畫面是一幅**模糊**的名畫。游標／手指**停住不動**，視野會慢慢**縮窄**、中心**解析度越來越高**；一移動就重置。
- 任務：找出藏在畫裡的 **3 隻貓**（貓是疊在名畫上的 sprite，會一起被模糊，對焦到才看得清）。
- **找到 = 對焦夠清晰時，把中心停在貓上約 0.35 秒**（自動認定，不用點選）。
- **超專注的代價**：你在對焦時，周邊會偶爾閃過小光點；因為你盯著中心、周邊又模糊，很容易**錯過**。結算會告訴你錯過了幾件。
- 找齊 3 隻貓過關，顯示「你錯過了 N 件周邊的事」。
- **手機支援**：畫以 cover 縮放後可能左右溢出螢幕（尤其直屏抓橫幅名畫）。單指**拖曳＝平移視野**，手指停住不動則交由既有的「停留偵測」自然接手開始對焦——同一個手勢自然銜接兩種操作，不需要額外的模式切換。畫面下方有一條位置條顯示目前視窗在整幅畫的哪一段。桌面滑鼠按住拖曳也能平移（窄視窗受益）。
- **看答案**：HUD 有一顆「看答案」鍵，可切換整張圖直接顯示清晰版（含貓），方便卡關時核對；不影響計時或找貓判定。

**可重用濾鏡 `FocusTunnel`**：輸入一張圖（可選 `decorate` 把疊加物烘進每層一起模糊），提供
`resize / update(dt, pointer) / render(ctx)`、`pan(dx, dy)` 平移、`revealed` 看答案旗標、
`focus` 狀態、`imgToScreen()` 座標轉換與 `overflowX` / `viewX` 供位置條使用。
離屏層 dpr 上限為 2（避免手機高 dpr 時三張模糊圖過大）；觸控裝置的「停留判定」閾值也放寬。
之後別的機制（或別的 prototype）可直接拿去用。

## 機制二：不在眼前就消失 (OutOfSight / 種花)

**經驗**：工作記憶像雜耍——沒在盯著的事，會從腦中枯萎、消失；你很難同時記住好幾件正在進行的事。

**玩法**：
- 場上有幾盆會緩慢隨機飄移的植物，游標／手指靠近＝「澆水」填滿進度條，一離開就開始枯萎。
- **完成後留下的殘影**：盆栽澆滿後，「真的完成了」的紀錄會收進 HUD 上方固定的一排小圖示（不佔場地、不會被誤找、永遠看得到）。但那株植物本身會變成一個半透明的**殘影**，繼續用原本的物理在場上飄——代表就算你已經完成了，那份「好像還沒收尾」的感覺，很多時候還是甩不掉、會在腦中一直飄。
- **關卡遞增**：`LEVELS` 設定表（盆數、飄移速度、枯萎速度、感應半徑、是否有干擾）分三關，一關比一關難記；每種植物池擴充到 6 種，各關取用不同數量。
- **干擾事件**（第 2 關起）：畫面隨機跳出 📳 通知，它在場上時**所有植物枯萎加倍**；停在通知上 0.4 秒可關掉，不理它會在 4 秒後自動消失並記一次「被干擾」，模擬「一個通知就打散你所有正在顧著的事」。
- **植物個性**：🌵 仙人掌怕澆太多——連續澆超過 2 秒反而爛根倒退；🌹 玫瑰枯得特別快；🌷 鬱金香要澆比較久；每種都有小提示字，逼你不能只用同一招應付所有植物。

## 機制三：最後一秒引擎 (Urgency)

**經驗**：平常什麼都啟動不了、慢半拍；但只要真的火燒屁股，反應速度會瞬間切換到完全不同的檔位。

**玩法**：
- 60 秒內盡量完成一排待辦（洗碗、回信、倒垃圾…）。平常按下去要等 0.5 秒才會生效——你感覺自己「慢半拍」。
- 剩最後 10 秒進入「腎上腺素」狀態：延遲消失，按下去立刻生效，畫面轉為警示紅、計時器脈動。
- 結算分開列出「平常完成幾件」與「最後 10 秒完成幾件」，讓你直接看到那個反差。

## 機制四：鑰匙放哪了 (LostKeys)

**經驗**：物件恆存感失靈——明明剛剛放下的東西，一轉頭做別的事、回頭就找不到了；「我很確定放在那」的自信常常對不上現實。

**玩法**（3 輪）：
1. **放置**：鑰匙 🔑 出現在 6 件家具其中一件上，記住它。
2. **打斷**：畫面跳出幾則通知，你得先點掉它們——這段時間你沒辦法盯著鑰匙的位置。
3. **重排＋殘影**：家具的螢幕位置重新洗牌；短暫閃過一個「殘影」——鑰匙原本所在的**舊位置**，格外有誤導性，因為家具早就不在那了。
4. **翻找**：點你覺得鑰匙所在的家具；點錯會用力搖晃提示、計入「翻找錯誤」，可以繼續猜到對為止。

結算顯示總翻找錯誤次數與最佳紀錄，並點出：家具沒有變、只是你腦中的地圖跟不上重排的速度。

## 範圍
### v0
- FocusTunnel 濾鏡 + 一幅名畫 + 3 隻貓 + 停住對焦/找到 + 周邊錯過機制 + 過關結算
- 首頁選單（可擴充）

### v1（本次）
- 種花：完成架、關卡遞增、干擾事件、植物個性
- 找貓：手機／窄視窗拖曳平移、視野位置條、看答案鍵、dpr 記憶體上限
- 新增機制：最後一秒引擎（urgency）、鑰匙放哪了（lostkeys）

### v2（之後，待挑選實作）
- 種花：更大場地（場地大於螢幕，得移動視角找盆栽，讓「不在眼前」變成字面意義）
- 更多症狀機制 idea：
  1. **時間盲「煮湯」**：顧爐上的湯同時玩小拼圖，沒有時鐘，靠體感回去關火；結算「你以為過了 X 秒，實際 Y 秒」。
  2. **啟動困難「就是按不下去」**：一顆大「開始」按鈕，游標靠近就冒出小事把你彈開；完成三步小儀式（助跑）才按得到。
  3. **分心跳題「讀不完的一段話」**：讀一段故事，字裡不斷長出閃亮連結，一點就掉進新內容，回來忘記讀到哪。
  4. **等待困難「紅燈手癢」**：等紅燈時畫面一直出現可點的小東西，點了扣分，但不點手會自己動。

## 技術
Svelte 5 + TS。FocusTunnel 用 Canvas 2D（離屏預算多層模糊 + 徑向遮罩柔邊隧道 + 平移），全程裝置像素運算。
種花／最後一秒引擎／鑰匙放哪了為 DOM + CSS transition 驅動，不用 canvas。
